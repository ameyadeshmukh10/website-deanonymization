{
  "name": "website-deanon-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker that captures visitor IPs and page views, stores them in KV, and mirrors to HubSpot Custom Behavioral Events.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "kv:list": "wrangler kv key list --binding IP_VISITS",
    "kv:get": "wrangler kv key get --binding IP_VISITS"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
