.env
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.pytest_cache/
.DS_Store

# Generated data — keep the folder, drop the contents
data/*.json
!data/.gitkeep
# Synthetic-test backup files
data/*.real.json

# Run logs — kept locally, not in repo
logs/*.log

# Cloudflare worker deps (installed via `npm install`)
cloudflaredeanonymizationsetup/node_modules/

# Claude Code local settings — contains tool-permission allowlists which may
# include credentials (e.g., Authorization headers from approved curl
# commands). Never commit. Each machine has its own copy.
.claude/settings.local.json

# macOS
.DS_Store
**/.DS_Store
